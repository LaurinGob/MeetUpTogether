<Window x:Class="MeetUpTogether.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MeetUpTogether.UI"
        xmlns:controls="clr-namespace:MeetUpTogether.UI.Controls"
        xmlns:icon="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MeetUp - Together!"
        Height="450"
        Width="800"
        Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <ToolBarTray Grid.Row="0" Grid.ColumnSpan="2">
            <ToolBar Background="#F0F0F0">
                <Button Command="{Binding GenerateReportCommand}" 
                    Width="120" Height="30"
                    Background="LightSkyBlue"
                    BorderBrush="#BFBFBF">
                    Generate PDF
                </Button>
                <!-- Add more toolbar buttons here later -->
            </ToolBar>
        </ToolBarTray>

        <controls:SearchBarUserControl Grid.Row="1" Grid.ColumnSpan="2" DataContext="{Binding SearchBar}"/>

        <controls:LeftPanelUserControl Grid.Row="2" Grid.Column="0" DataContext="{Binding LeftPanel}"/>
        <controls:RightPanelUserControl Grid.Row="2" Grid.Column="1" DataContext="{Binding RightPanel}"/>
    </Grid>
</Window>